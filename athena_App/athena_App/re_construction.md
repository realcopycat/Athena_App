# 重构说明

本项目将分为三个层次

- 底层数据库操作层：layer_dataOperating   
负责数据库的写入、查询和更新操作  
另外，本次重构抛弃AIML方法，充分利用ES已有的分词查询功能


- 前端交互处理层：layer_frontInteracting  
负责接受前端数据，被视图函数所调用。调用数据库操作层来获取输出数据

- 前端Web应用层：本层是flask应用层  
负责生成页面，直接与用户交互
